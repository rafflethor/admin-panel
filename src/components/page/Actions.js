import React from 'react'

const Actions = (props) => (
    <div className="row page-actions">
        <div className="col-12">
            {props.children}
        </div>
    </div>
)

export default Actions
